import { ComponentStyleConfig } from '@chakra-ui/react';

export const textStyles: ComponentStyleConfig = {
  baseStyle: {
    fontSize: '1rem'
  },
  variants: {
    italic: {
      fontSize: '36px',
      fontStyle: 'italic'
    }
  }
};
